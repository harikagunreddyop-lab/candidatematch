version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - npm ci
    build:
      commands:
        # Optional: write selected env vars into .env.production for Next.js
        - env | grep -e NEXT_PUBLIC_ >> .env.production || true
        - env | grep -e SUPABASE_SERVICE_ROLE_KEY >> .env.production || true
        - env | grep -e ANTHROPIC_API_KEY >> .env.production || true
        - env | grep -e APIFY_API_TOKEN >> .env.production || true
        - env | grep -e CRON_SECRET >> .env.production || true
        - env | grep -e RESUME_WORKER_URL >> .env.production || true
        - env | grep -e AUTOFILL_ALLOWED_ORIGINS >> .env.production || true
        - env | grep -e NEXT_PUBLIC_APP_URL >> .env.production || true
        - env | grep -e GMAIL_ >> .env.production || true
        # Validate required env vars are present in the Amplify environment
        - |
          echo "Validating required env vars..."
          missing=0
          for var in NEXT_PUBLIC_SUPABASE_URL SUPABASE_SERVICE_ROLE_KEY ANTHROPIC_API_KEY CRON_SECRET; do
            if [ -z "${!var}" ]; then
              echo "FATAL: $var is missing from Amplify environment"
              missing=1
            fi
          done
          [ "$missing" -eq 0 ] || exit 1
          echo "All required env vars present."
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
