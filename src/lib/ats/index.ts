/**
 * Legacy ATS scorer types.
 *
 * Runtime scoring has been migrated to ATS Engine v3 (`@/lib/ats-v3`).
 * These exports are kept for type compatibility only.
 */
export type {
  JobRequirements,
  ResumeCandidate,
  ATSScoreResult,
  DimensionResult,
  KeywordCoverageResult,
  ParseIntegrityResult,
} from './types';
